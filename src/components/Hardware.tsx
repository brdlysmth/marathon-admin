import React from "react";
import { Layout, Menu, Breadcrumb } from "antd";
const { Header, Content, Footer, Sider } = Layout;

const Hardware: React.FC = () => {
  return <Content> Hardware </Content>;
};

export default Hardware;
